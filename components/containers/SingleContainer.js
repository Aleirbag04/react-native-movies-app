import { View, Text, Image } from 'react-native';

const SingleContainer = ({ navigation, route }) => {
//const {id, category} = route.params;

return(
    <View>
        <Text>Single View Screen</Text>
     </View>)
}

export default SingleContainer;