import SingleContainer from '../containers/SingleContainer';

const SingleViewScreen = ({ navigation, route }) => <SingleContainer navigation={navigation} route={route} />;
export default SingleViewScreen;